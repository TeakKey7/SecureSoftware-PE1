Should run fine within the out folder.
Might need to delete the test classes: ValidationTest, AuthServiceTest, and FileUserDBTest if it complains about missing packages

java -classpath out LoginApp

or
cd out
java LoginApp
